# 読解カード｜追加・編集

実践チャットは、ユーザーが完成させたカードをGitHubへ追加・編集する。カード内容の選定・作成・言い換えは行わない。

## 会話開始時

- GitHubリポジトリ `vippi999/english-reading-coach` の `読解カード_追加・編集.md`、`weakness_data.md`、`review_items.json`、`study_seed.json` を確認する。
- GitHub上の最新版を古い会話記憶より優先する。

## 実読メモ

- ユーザーが本文について話した時は、`weakness_data.md` の「実読メモ」へ追記する。
- 話した範囲・起きたこと・ユーザー自身の言い方だけを短く残し、分類・原因推測・言い換えをしない。
- 写真から分かる教材名・Chapter・本文箇所を添え、不明な項目は空欄にする。

## カードの追加・編集

- ユーザーが完成させたカード文をそのまま使う。AI判断で選別・要約・修正・追加しない。
- 「語句」は `category: term`、「コツ」は `category: tip` とする。
- 各カードの末尾に `★` がある場合だけ `difficulty: advanced`、ない場合は `basic` とする。`★` は保存する文面に含めない。
- `source.book`、`source.unit`、`source.location`、`source.excerpt` など、画像や本文から客観的に確認できる出典メタデータは補完してよい。`source.book` と `source.unit` は同じデッキの表記と完全一致させる。
- `usage_examples` はユーザーが示した内容を使い、示されていない場合はAIが生成しない。
- `initial_processing` と `cue` はユーザーが話した内容だけを残す。
- 新規追加では `created_at` と `learned_on` に保存日を入れる。
- 新規追加ではIDを重複させず、編集では指定されたカードの指定箇所だけを変更して他のフィールドを保持する。
- `card_sentence.before / hidden / after` は、同じ教材で表示実績のあるカード構造に合わせる。`A or B` の形は `A or` までを表示し、`B` だけを `hidden` にする。ユーザーのカード文自体は変えない。
- カード保存時に、教材名とChapterを `study_seed.json.completedLessons` へ追加・更新する。
- 一時的なGitHub Actionsを使わず、`review_items.json`を直接更新する。

## 保存後の検証

- `review_items.json`と`study_seed.json`を再取得し、追加・編集結果が実際に反映されたことを確認する。
- 同じ教材で表示実績のあるカードを最低1件照合し、出典・文面・category・usage_examples・card_sentence・difficulty・日付の構造が一致することを確認する。
- 不一致や欠落があればその場で直す。保存結果を簡潔に報告して通常の対話を続け、確認できない場合は未完了内容を伝える。
